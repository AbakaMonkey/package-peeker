[build-system]
requires = ['setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "package-peeker"
version = "0.1.0"

dependencies = [
	"simple_term_menu"
]

[project.scripts]
package-peeker = "package_peeker.core:main"
